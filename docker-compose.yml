services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-solutions
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    networks:
      - ai-solutions

networks:
  ai-solutions:
    driver: bridge